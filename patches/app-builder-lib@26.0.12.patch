diff --git a/out/node-module-collector/index.js b/out/node-module-collector/index.js
index 7d0ad02adecdce2ea5e3ebd1f4f44f840e77eb1b..c6e7cc455ab1bc565412fc0c886ce29161740829 100644
--- a/out/node-module-collector/index.js
+++ b/out/node-module-collector/index.js
@@ -20,6 +20,10 @@ async function getCollectorByPackageManager(rootDir) {
     const manager = await (0, packageManager_1.detect)({ cwd: rootDir });
     switch (manager) {
         case "pnpm":
+            // Upstream behavior kept: with node-linker=hoisted the layout is npm-like and
+            // pnpm list reports virtual-store paths that don't exist, so npm must walk it.
+            // (Requires a "workspaces" field in the repo root package.json so npm ls can
+            // parse workspace:* specifiers — see HANDOFF.md in the Leapp fork.)
             if (await isPnpmProjectHoisted(rootDir)) {
                 return new npmNodeModulesCollector_1.NpmNodeModulesCollector(rootDir);
             }
diff --git a/out/node-module-collector/packageManager.js b/out/node-module-collector/packageManager.js
index 7c566618ede3c26fd82bdfa9ff061738fdbcfea1..7bc06200dbbedfa6ca0fd5d0ee73b4a0021ffeb9 100644
--- a/out/node-module-collector/packageManager.js
+++ b/out/node-module-collector/packageManager.js
@@ -50,7 +50,10 @@ function getTypeofLockFile(cwd = process.cwd()) {
             value = "bun";
         }
         else {
-            value = "npm";
+            // PATCHED (Leapp fork): return null instead of "npm" when no lockfile exists in
+            // this directory, so detect()'s parent-directory walk (monorepo support) actually
+            // runs instead of being dead code
+            value = null;
         }
         cache.set(key, value);
         return value;
