# npm-like flat node_modules: this legacy Electron/Angular toolchain (and
# electron-builder's node_modules/** packaging globs) expects a hoisted layout,
# not pnpm's default symlinked store.
node-linker=hoisted
